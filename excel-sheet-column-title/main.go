package main

import (
	"fmt"
	"math"
)

func main(){
	fmt.Println(string(66))
	convertToTitle(7001)
}

func convertToTitle(columnNumber int) string {
    fmt.Println(math.Sqrt(float64(columnNumber)))
	return ""
}